import styled from "styled-components";

export const TicketDataWrapper = styled.div`
  width: 100%;
  .ticket-title {
    font-size: 18px;
    color: var(--text-2);
  }
  .ticket-buttons {
    width: 100%;
    margin-top: 30px;
    display: flex;
    gap: 100px;
  }
`;
